@model IEnumerable<myblogNew.Models.Menu>
@{ var culture = Request.Cookies["culture"]; }
<!-- Navigation -->
<!--<nav class="navbar navbar-expand-lg navbar-light fixed-top" id="mainNav">
    <div class="container">
    @if (culture.Value == "tr")
    {
        foreach (var item in ViewBag.ayarlar)
        {
            <a class="navbar-brand" href="/">@item.SiteAdi</a>
        }
    }
    else if (culture.Value == "en") {
       foreach (var item in ViewBag.ayarlar)
        {
            <a class="navbar-brand" href="/">@item.SiteAdiEng</a>
        }
    }


        <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            Menu
            <i class="fas fa-bars"></i>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
            <ul class="navbar-nav ml-auto">
                @foreach (var item in Model)
                {
                    <li class="nav-item">
                        <a class="nav-link" href="@item.Link">@item.Adi</a>
                    </li>
                }
            </ul>
        </div>
    </div>
</nav>-->
<!-- Page Header -->
@{ if (culture.Value == "en")
    {
<nav id="navbar" class="navbar">



    <ul>
        <li><a class="nav-link scrollto active" href="#hero">Home</a></li>
        <li><a class="nav-link scrollto" href="#about">About</a></li>
        <li><a class="nav-link scrollto" href="#services">Services</a></li>
        <li><a class="nav-link scrollto " href="#work">Work</a></li>
        <li><a class="nav-link scrollto " href="#blog">Blog</a></li>
        <li><a class="nav-link scrollto" href="#contact">Contact</a></li>
        <li class="dropdown">
            <a href="#"><span>Change Language</span> <i class="bi bi-chevron-down"></i></a>
            <ul>


                @{ foreach (var i in myblogNew.Controllers.LanguageMang.AvailableLanguages)
                    {
                        //Html.ActionLink(i.LanguageFullName, "ChangeLanguage", "Blog", new { lang = i.LanguageCultureName }, null);
    <li>@Html.ActionLink(i.LanguageFullName, "ChangeLanguage", "Blog", new { lang = i.LanguageCultureName }, null)</li>
    @*<li><a href="@Html.ActionLink("ChangeLanguage", "Blog", new { lang = i.LanguageCultureName })">i.LanguageCultureName</a></li>*@ } }
            </ul>
        </li>
    </ul>
    <i class="bi bi-list mobile-nav-toggle"></i>
</nav><!-- .navbar --> }
else if (culture.Value == "tr")
{
<nav id="navbar" class="navbar">



    <ul>
        <li><a class="nav-link scrollto active" href="#hero">Anasayfa</a></li>
        <li><a class="nav-link scrollto" href="#about">Hakkımızda</a></li>
        <li><a class="nav-link scrollto" href="#services">Hizmetlerimiz</a></li>
        <li><a class="nav-link scrollto " href="#work">Referanslar</a></li>
        <li><a class="nav-link scrollto " href="#blog">Blog</a></li>
        <li><a class="nav-link scrollto" href="#contact">İletişim</a></li>
        <li class="dropdown">
            <a href="#"><span>Dili Degiştir</span> <i class="bi bi-chevron-down"></i></a>
            <ul>


                @{ foreach (var i in myblogNew.Controllers.LanguageMang.AvailableLanguages)
                    {
                        //Html.ActionLink(i.LanguageFullName, "ChangeLanguage", "Blog", new { lang = i.LanguageCultureName }, null);
    <li>@Html.ActionLink(i.LanguageFullName, "ChangeLanguage", "Blog", new { lang = i.LanguageCultureName }, null)</li>
    @*<li><a href="@Html.ActionLink("ChangeLanguage", "Blog", new { lang = i.LanguageCultureName })">i.LanguageCultureName</a></li>*@ } }
            </ul>
        </li>
    </ul>
    <i class="bi bi-list mobile-nav-toggle"></i>
</nav><!-- .navbar --> } }

